---
- name: Hello World
  hosts: all
  sources:
    - ansible.eda.range:
        limit: 2
  rules:
    - name: Say Hello
      condition: event.i == 1
      action:
        debug:
          msg: "User is: '{{userid}}' with password: '{{password}}'"
